pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: pnpm exec biome check --write --formatter-enabled=true --linter-enabled=true --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    - name: markdown
      glob: "*.md"
      run: pnpm exec markdownlint-cli2 --fix {staged_files}
      stage_fixed: true
    - name: yaml
      glob: "*.{yml,yaml}"
      run: yamllint -c .yamllint {staged_files}
    - name: actions
      glob: ".github/workflows/*.{yml,yaml}"
      run: actionlint -shellcheck= {staged_files}
    - name: workflow-pins
      glob: ".github/workflows/*.{yml,yaml}"
      run: node scripts/check-workflow-pins.mjs
    - name: toml
      glob: "*.toml"
      run: taplo fmt {staged_files}
      stage_fixed: true
    - name: rustfmt
      glob: "*.rs"
      run: rustfmt {staged_files}
      stage_fixed: true

pre-push:
  jobs:
    - name: format:check
      run: mise run format:check
    - name: test:unit:ci
      run: mise run test:unit:ci
    - name: lint
      run: mise run lint
    - name: test:rust
      run: mise run test:rust
    - name: build
      run: mise run build
